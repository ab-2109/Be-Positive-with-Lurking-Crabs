- Shrey, in the file_ops.cpp file, while read and write, append the subfolder 
(something like BE-POSITIVE-...-CRABS/Nodes/) before the txt file name.
After you do this, delete this point from this file.

- In the main function, we can make menu based insert/search/delete --DONE
options. 
    Note: In case of failure, we will print error msg according to 
    BP_ERROR value.
    If user chooses insert:
        We will ask for key.
        We will ask for all the field values.--OMITTED
        Call the Insert function.
        If insertion is successful, we will create a txt file corresponding 
        to that row entry as decided. And print success msg.
        If not, print failure msg with the reason being memory is full 
        (cuz that is the only reason we are returning false in insert).

    If user chooses delete:
        We will ask for key.
        If key doesn't exist, print error msg.
        If key exists, delete the key's corresponding row txt file and 
        print success msg.