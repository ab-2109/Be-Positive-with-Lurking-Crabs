The order of our B+ Tree is n = 8.
We will allow a maximum of 6 levels (due to storage limits of our laptops).
Thus, names of nodes (which are text files) will be an integer in the range 1 to 37449 
(as 1+8+8^2+...+8^5 = 37449).

Each row entry will be stored in an independent text file named after it's key.

There can be a maximum of 8^5 leaf nodes. We are capping the total number of sql entries 
at 131072 (which is 4*8^5) because in the worst case all the leaf nodes will have 4 entries 
but if just one of them has 8 entries and we have to add the 9th one there, there will be a need 
to increase the number of levels.


Priority Queue: For available file names (initially add 1 to 37449). Initially all these nums 
are added to the pq. Whenever we want to insert an element, we will do pq.pop() and whenever we 
delete a file, we will add the integer to the pq.

We will make 2 folders, one to store node txt files and the other to store sql row files.
which are index and data respectively

If the node is p1 k1 p2 k2 ... pn, then, max(p_i)<k_i<=min(p_i+1).

Assumption: Key is always >0
In real life, if it is non-zero, we can create a function to map actual 
keys to B+ Tree Keys so that the keys become positive integers.

In case of insert, if the key already exists, it's file will be over-written.

File structure:
    For non-leaf node:
        0
        p1 k1 p2 k2 ... p_n
    
    For leaf node: (p_i points to the file storing entry corresponding to )
        1
        k1 p1 k2 p2 ... k_n p_n
